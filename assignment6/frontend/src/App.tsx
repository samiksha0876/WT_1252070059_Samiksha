import {BrowserRouter,Routes,Route} from 'react-router-dom'; import {useState} from 'react'; import type {Book} from './types/book'; import Home from './pages/Home'; import Catalogue from './pages/Catalogue'; import Login from './pages/Login'; import Register from './pages/Register'; import Dashboard from './pages/Dashboard'; import Navbar from './components/Navbar'; import Footer from './components/Footer';
export default function App(){const [cart,setCart]=useState<Book[]>([]); const add=(b:Book)=>setCart(c=>[...c,b]); return <BrowserRouter><Navbar cartCount={cart.length}/><main><Routes><Route path="/" element={<Home onAdd={add}/>}/><Route path="/catalogue" element={<Catalogue onAdd={add}/>}/><Route path="/login" element={<Login/>}/><Route path="/register" element={<Register/>}/><Route path="/dashboard" element={<Dashboard cart={cart}/>}/></Routes></main><Footer/></BrowserRouter>}
