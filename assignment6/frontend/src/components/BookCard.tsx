import {ShoppingBag,Star} from 'lucide-react'; import type {Book} from '../types/book';
export default function BookCard({book,onAdd}:{book:Book;onAdd:(b:Book)=>void}){return <article className="book-card"><div className="cover"><img src={book.coverUrl} onError={e=>(e.currentTarget.style.display='none')} /><span>{book.category}</span></div><div className="book-info"><h3>{book.title}</h3><p>{book.author}</p><div className="rating"><Star size={14} fill="currentColor"/> {book.rating}</div><div className="price-row"><b>₹{book.price}</b><button onClick={()=>onAdd(book)}><ShoppingBag size={15}/> Add to Cart</button></div></div></article>}
