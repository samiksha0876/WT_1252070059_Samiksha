import {Link,useNavigate} from 'react-router-dom'; import {BookOpen,Search,ShoppingBag,UserRound,Menu,X} from 'lucide-react'; import {useState} from 'react';
export default function Navbar({cartCount=0}:{cartCount?:number}){const [open,setOpen]=useState(false); const [q,setQ]=useState(''); const nav=useNavigate(); const submit=(e:React.FormEvent)=>{e.preventDefault(); nav(`/catalogue?q=${encodeURIComponent(q)}`);setOpen(false)}; return <header className="nav"><Link to="/" className="brand"><span className="brand-icon"><BookOpen size={23}/></span><span><b>BookNest</b><small>Read More, Be More</small></span></Link><button className="menu" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button><nav className={open?'links show':'links'}><Link to="/">Home</Link><Link to="/catalogue">Books</Link><Link to="/catalogue?category=Fiction">Categories</Link><Link to="/dashboard">About</Link></nav><form className="search" onSubmit={submit}><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search books, authors..."/><button><Search size={17}/></button></form><Link className="nav-login" to="/login"><UserRound size={18}/> Login</Link><Link className="cart" to="/catalogue"><ShoppingBag size={20}/>{cartCount>0&&<i>{cartCount}</i>}</Link></header>}
