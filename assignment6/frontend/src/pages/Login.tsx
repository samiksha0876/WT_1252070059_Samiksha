import {Link,useNavigate} from 'react-router-dom'; import {BookOpen,Mail,Lock,Eye,EyeOff} from 'lucide-react'; import {useState} from 'react'; import {login} from '../services/api';
export default function Login(){const nav=useNavigate();const [show,setShow]=useState(false);const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();setError('');try{const r=await login({email,password});localStorage.setItem('booknestUser',JSON.stringify(r));nav('/dashboard')}catch(err){setError((err as Error).message)}};return <section className="auth-page"><div className="auth-art"><BookOpen size={50}/><h2>Good Books.<br/><em>Good Life.</em></h2><p>“There is no friend as loyal as a book.”</p></div><div className="auth-card"><p className="eyebrow">WELCOME BACK</p><h1>Sign in to BookNest</h1><p className="muted">Continue your reading journey.</p><form onSubmit={submit}><label>Email</label><div className="input"><Mail size={17}/><input required type="email" placeholder="Enter your email" value={email} onChange={e=>setEmail(e.target.value)}/></div><label>Password</label><div className="input"><Lock size={17}/><input required type={show?'text':'password'} placeholder="Enter your password" value={password} onChange={e=>setPassword(e.target.value)}/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff size={17}/>:<Eye size={17}/>}</button></div>{error&&<p className="error">{error}</p>}<button className="primary full">Login</button></form><div className="or">OR</div><button className="social">Continue with Google</button><p className="switch">Don't have an account? <Link to="/register">Register here</Link></p></div></section>}
