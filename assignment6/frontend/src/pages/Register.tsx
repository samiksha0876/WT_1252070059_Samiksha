import {Link,useNavigate} from 'react-router-dom'; import {UserRound,Mail,Lock,BookOpen} from 'lucide-react'; import {useState} from 'react'; import {register} from '../services/api';
export default function Register(){const nav=useNavigate();const [form,setForm]=useState({fullName:'',email:'',password:''});const [confirm,setConfirm]=useState('');const [error,setError]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();if(form.password!==confirm){setError('Passwords do not match');return}try{await register(form);nav('/login')}catch(err){setError((err as Error).message)}};return <section className="auth-page"><div className="auth-art"><BookOpen size={50}/><h2>Good Books.<br/><em>Good Life.</em></h2><p>Build a library that follows your curiosity.</p></div><div className="auth-card"><p className="eyebrow">JOIN BOOKNEST</p><h1>Create your account</h1><p className="muted">Save favorites and keep your reading journey in one place.</p><form onSubmit={submit}><label>Full Name</label><div className="input"><UserRound size={17}/><input required placeholder="Enter your full name" value={form.fullName} onChange={e=>setForm({...form,fullName:e.target.value})}/></div><label>Email</label><div className="input"><Mail size={17}/><input required type="email" placeholder="Enter your email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></div><label>Password</label><div className="input"><Lock size={17}/><input required type="password" placeholder="Create a password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></div><label>Confirm Password</label><div className="input"><Lock size={17}/><input required type="password" placeholder="Confirm your password" value={confirm} onChange={e=>setConfirm(e.target.value)}/></div>{error&&<p className="error">{error}</p>}<button className="primary full">Register</button></form><p className="switch">Already have an account? <Link to="/login">Login here</Link></p></div></section>}
